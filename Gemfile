# frozen_string_literal: true

source 'https://rubygems.org'
ruby '2.6.2'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# RSpec meta-gem that depends on the other components
gem 'rspec'
# The instafailing RSpec progress bar formatter
gem 'fuubar'
# A Ruby static code analyzer and formatter, based on the community Ruby style guide
gem 'rubocop', require: false
# RSpec-specific analysis for your projects, as an extension to RuboCop.
gem 'rubocop-rspec', require: false
# A runtime developer console and IRB alternative with powerful introspection capabilities
gem 'pry'
# Code coverage for Ruby 1.9+ with a powerful configuration library
# and automatic merging of coverage across test suites
gem 'simplecov', require: false
# A more accurate picture of your code usage. In particular a line is considered covered
# if and only if it is entirely executed
gem 'deep-cover', '~> 0.7', require: false
